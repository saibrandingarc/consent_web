name: Build and deploy consent_web

on:
  push:
    branches: [dev, main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22.x'
          package-manager-cache: false
      - run: corepack enable && corepack prepare pnpm@12.5.1 --activate
      - name: Build
        env:
          NEXT_PUBLIC_API_URL: ${{ vars.API_URL || 'https://consentapi-abgrbph5cfccbxe0.eastus2-01.azurewebsites.net' }}/api/v1
          APP_BASE_URL: ${{ vars.WEB_URL || 'https://consentmngtdev-gtfgamd4c9b4bbcr.eastus2-01.azurewebsites.net' }}
          WEB_URL: ${{ vars.WEB_URL || 'https://consentmngtdev-gtfgamd4c9b4bbcr.eastus2-01.azurewebsites.net' }}
          ADMIN_URL: ${{ vars.ADMIN_URL || 'https://consentadmin-fwb7gmeybmhwhyd0.eastus2-01.azurewebsites.net' }}
          AUTH0_SECRET: build-time-placeholder-secret-32chars
          AUTH0_DOMAIN: example.auth0.com
          AUTH0_CLIENT_ID: build-placeholder-client-id
          AUTH0_CLIENT_SECRET: build-placeholder-client-secret
        run: |
          pnpm install
          pnpm build
          pnpm --filter=consent_web deploy --prod ./azure-web
          bash deploy/azure/assemble.sh
      - uses: azure/webapps-deploy@v3.0.8
        with:
          app-name: ${{ vars.AZURE_WEB_APP || 'consentmngtdev' }}
          package: azure-site-web
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE || secrets.AZUREAPPSERVICE_PUBLISHPROFILE_EC3C9EE888A64CDD988535812D7EA589 }}
